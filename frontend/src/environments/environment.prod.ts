export const environment = {
  production: true,
  // Replace with your Render backend URL after the service is created,
  // e.g. 'https://homelink-backend.onrender.com'.
  apiUrl: 'https://REPLACE_WITH_RENDER_BACKEND_URL'
};
